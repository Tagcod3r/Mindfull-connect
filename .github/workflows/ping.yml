name: Keep Render App Alive

on:
  schedule:
    # This runs once every hour (CRON format in UTC)
    - cron: '0 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping the Render App
        run: curl --silent --show-error https://mindfull-connect.onrender.com
